# 开发工作流程规范

## 🎯 目标

支持高效的两阶段开发模式：先快速实现功能验证，再进行规范化改造，确保最终交付符合项目标准。

## 🔄 推荐的两阶段开发模式

**支持"功能实现后再进行规范改造"的开发流程：**

### 🚀 Phase A: 快速功能实现阶段
```bash
# ✅ 快速实现流程（使用标准组件）
1. 使用 a-table、a-modal、a-form 等标准Ant Design组件快速实现功能
2. 专注于业务逻辑和数据流的正确性
3. 确保功能完整性和基本的用户交互
4. 进行基础的功能测试验证
```

### 🎯 Phase B: 规范改造阶段  
```bash
# ✅ 规范改造流程（升级为DAS组件）
1. 学习项目DAS组件库规范
2. 按优先级替换组件：a-table → das-table
3. 调整页面结构符合页面布局标准
4. 优化样式和交互细节
5. 进行规范合规性验证
```

**两阶段开发的优势：**
- 快速验证业务逻辑和功能可行性
- 降低初期开发的学习成本和时间投入
- 分离关注点：先关注功能，再关注规范
- 可以在功能验证通过后再投入规范改造成本

## 📋 两阶段开发流程

### 阶段 A: 快速功能实现 🚀

**目标：使用标准组件快速完成功能开发**

**Phase A1: 快速设计**
1. **基本技术选型**
   ```typescript
   // ✅ 快速实现时可使用的组件
   表格: a-table
   弹窗: a-modal  
   表单: a-form
   按钮: a-button
   输入框: a-input, a-select
   卡片: a-card
   ```

2. **简化页面结构**
   ```vue
   <!-- ✅ 快速实现的基本结构 -->
   <div class="[module-name]-page">
     <a-card>
       <!-- 搜索表单 -->
       <a-form>
       
       <!-- 数据表格 -->
       <a-table>
       
       <!-- 操作弹窗 -->
       <a-modal>
   ```

**Phase A2: 功能实现**
- 专注业务逻辑正确性
- 实现完整的CRUD操作
- 基本的表单验证
- 数据加载和错误处理

**Phase A3: 功能验证**
- 所有功能可以正常使用
- 数据流程验证无误
- 基本的用户交互测试

---

### 阶段 B: 规范改造升级 🎯

**目标：将功能代码改造为符合项目规范的高质量代码**

**Phase B1: 规范学习**
1. **必读规范文档**
   ```bash
   .cursor/rules/README.mdc                    # 规范体系概览
   .cursor/rules/component-library-standards.mdc  # DAS组件库规范
   das-components-folder/das-component-vue.json  # 可用组件清单
   ```

2. **功能特定规范**
   ```bash
   列表页面 → .cursor/rules/list-page-rule.mdc
   表单页面 → .cursor/rules/form-validation-standards.mdc  
   API集成 → .cursor/rules/api-request-standards.mdc
   ```

**Phase B2: 组件升级**
1. **组件替换映射**
   ```typescript
   // 🔄 组件升级映射表
   a-table     → das-table
   a-form搜索  → das-search-bar
   a-form表单  → 保持a-form但优化结构
   ```

2. **页面结构规范化**
   ```vue
   <!-- ✅ 规范的页面结构 -->
   <div class="[module-name]-container">
     <a-card style="height: 100%">
       <!-- 搜索区域 -->
       <div class="search-section">
         <das-search-bar>
       
       <!-- 数据展示区域 -->
       <div class="data-display-section">
         <das-table>
   ```

**Phase B3: 质量提升**
- 完善TypeScript类型定义
- 优化样式和响应式设计
- 增强表单验证和错误处理
- 添加加载状态和用户反馈

**Phase B4: 合规验证**
- 对比规范文件检查合规性
- 组件使用符合优先级要求
- 页面结构符合布局标准
- 通过所有代码质量检查

## 🔧 工具和检查机制

### 自动检查脚本
```bash
# 建议添加的项目检查脚本
npm run check:compliance    # 检查组件使用合规性
npm run check:structure     # 检查页面结构规范
npm run check:types         # TypeScript类型检查
```

### 违规警告系统
```javascript
// 建议的ESLint规则
"rules": {
  "no-antd-table": "error",     // 禁止使用a-table
  "no-antd-modal": "error",     // 禁止使用a-modal
  "require-das-components": "warn" // 警告未使用DAS组件
}
```

## 🔄 阶段转换管理

### 阶段A到阶段B的转换条件
必须满足以下条件才能从快速实现转向规范改造：
1. **功能完整性验证** - 所有CRUD操作正常工作
2. **数据流程验证** - API调用和数据处理无误
3. **基本交互验证** - 用户界面交互逻辑正确
4. **无重大错误** - 没有阻断性的功能问题

### 阶段B的完成标准
规范改造阶段必须达到以下标准：
1. **组件规范化** - 完成a-table→das-table等组件升级
2. **结构规范化** - 页面结构符合layout标准
3. **类型安全** - 完善TypeScript类型定义
4. **代码质量** - 通过ESLint和项目质量检查
5. **响应式设计** - 适配不同屏幕尺寸

### 质量控制要求
- 阶段A可以使用标准组件，但不得降低代码结构质量
- 阶段B必须完成所有规范改造，不得保留非规范组件
- 两个阶段都必须保持TypeScript类型安全
- 最终交付必须100%符合项目规范

## 📊 两阶段成功标准

### 阶段A成功标准（快速实现）
1. ✅ 功能完整性：所有CRUD操作正常
2. ✅ 数据流正确：API调用和状态管理无误  
3. ✅ 基本交互：用户界面响应正常
4. ✅ 无重大错误：没有阻断性问题
5. ✅ 代码结构清晰：便于后续规范改造

### 阶段B成功标准（规范改造）
1. ✅ 100% 完成组件升级（a-table→das-table等）
2. ✅ 100% 符合页面布局规范  
3. ✅ 100% 通过TypeScript类型检查
4. ✅ 100% 通过项目linting检查
5. ✅ 响应式设计和用户体验优化完成

## 🔄 流程优势

这种两阶段开发模式的优势：
- **快速验证**：在投入大量规范学习成本前先验证功能可行性
- **分离关注**：阶段A专注业务逻辑，阶段B专注代码质量
- **降低风险**：避免学习成本浪费在不可行的功能上
- **灵活调整**：在功能验证后可以根据需求调整规范改造范围
description:
globs:
alwaysApply: false
---
